import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#232f3e', 
    },
    secondary: {
      main: '#dc004e', 
    },
    background: {
      paper: '#f6f6f6', 
    },
  },
});

export default theme;

